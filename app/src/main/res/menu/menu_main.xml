<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="tv.hillsbrad.com.tvprogramlist.MainActivity">
    <item
        android:id="@+id/action_custom_channel_settings"
        android:orderInCategory="100"
        android:title="@string/action_custom_channel_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/use_fake_data"
        android:orderInCategory="120"
        android:title="use fake data: on"
        app:showAsAction="never"
        android:visible="true"/>

    <item
        android:id="@+id/refresh_on_parse"
        android:orderInCategory="130"
        android:title="refresh on update: off"
        app:showAsAction="never"
        android:visible="true"/>
</menu>
